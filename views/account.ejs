<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  <link rel="stylesheet" href="../static/styles.css">
  <title>TaskEmbarraser | Account</title>
</head>
<%- include("header.ejs") -%>
<body>
  <div id="welcome">
    <h1>Your TaskEmbarraser Account</h1>
  </div>
  <% if (fieldUpdatedSuccessfully !== null) { %>
  <div class="alert alert-success" role="alert" id="success-field">
    <% if (fieldUpdatedSuccessfully === "email") { %>
      Email Sucessfully Updated 
    <% } else if (fieldUpdatedSuccessfully === "recieveEmails") { %>
      Recieve Emails In Successfully Updated 
    <% } else { %>
      Password Successfully Change
    <% } %>
  </div>
  <% } %>
  <div id="form">
    <form action="/edit-recieveEmails" method="POST">
      <div class="account-item">
        <button class="button" id="email-button" type="button" onclick="editEmailGET()">Edit</button>
        <p>Your Email: <%= currentEmail %></p>
      </div>
        <div class="account-item">
          <button class="button" id="staySignedIn-button" type="submit">Save</button>
          <div class="mb-3 form-check">
            <label class="form-check-label" for="recieveEmails">Recieve Emails</label>
            <input name="recieveEmails" value="1" type="checkbox" class="form-check-input" id="recieve-emails"
            <% if (check) { %>
              checked
            <% } %>
            >
          </div>
          <div class="form-text" id="email-info">*recieve emails about new and popular recipes</div>
        </div>
      <button id="change-password-button" type="button" class="button" onclick="changePasswordGET()">Change Password</button>
      <button id="delete-account-button" type="button" class="button" onclick="deleteAccountGET()">Delete Account</button>
    </form>   
  </div>
</body>
<script src="../static/index.js" charset="UTF-8"></script>
</body>
</html>