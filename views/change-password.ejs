<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  <link rel="stylesheet" href="../static/styles.css">
  <title>TaskEmbarraser | Change Password</title>
</head>
<%- include("header.ejs") -%>
<body>
  <div id="welcome">
    <h1>Change Your Password</h1>
  </div>
  <div id="form">
    <form action="/change-password" method="POST" autocomplete="on">
      <div class="mb-3">
        <label for="current-password" class="form-label">Password</label>
        <input autocomplete="current-password" name="currentPassword" type="password" class="form-control" id="current-password">
      </div>
      <div class="mb-3">
        <label for="new-password" class="form-label">New-Password</label>
        <input autocomplete="new-password" name="newPassword" type="password" class="form-control" id="new-password">
      </div>
      <div class="mb-3">
        <label for="verify" class="form-label">Verify-Password</label>
        <input autocomplete="new-password" name="verify" type="password" class="form-control" id="verify">
      </div>
      <% if (allFieldsRequired || incorrectPassword || passwordsDoNotMatch) { %>
        <div class="alert alert-danger" role="alert">
        <% if (allFieldsRequired) { %> 
          All Fields Required
        <% } else if (incorrectPassword) { %>
          Incorrect Password
        <% } else { %>
          Passwords must match
        <% } %>
        </div>
      <% } %>
      <div class="buttons-grid">
        <button class="button" type="button" id="cancel-password-button" onclick="accountGET()">Cancel</button>
        <button class="button" type="submit" id="save-password-button">Save</button>
      </div>
    </form>
  </div>
</body>
<script src="../static/index.js" charset="UTF-8"></script>
</html>